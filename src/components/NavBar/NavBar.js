import Navigation from './Navigation';
import { Header } from './NavBar.Styled';

export default function NavBar() {
    return (
        <Header >
            <Navigation />
        </Header>
    );
}